import { Controls } from "./controls.js";

export class Tank {
	constructor() {
		// this.controls = new Controls(true);
	}

	update() {
		// console.log(this.controls.getAction("forward"));

	}

	/**
	* @param {CanvasRenderingContext2D} ctx
	* @param {HTMLCanvasElement} c - canvas
	*/
	render(ctx, c) {

	}
}